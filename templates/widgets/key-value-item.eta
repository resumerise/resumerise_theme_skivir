<div class="key-value-item" id="<%= it.id %>">
<% if(it.label) { %>
    <label class="label">
        <%= it.label %>
    </label>
<% } %>
<% if(it.value) { %>
    <span class="span">
        <%= it.value %>
    </span>
<% } %>
</div>