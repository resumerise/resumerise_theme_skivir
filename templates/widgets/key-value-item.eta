<div class="key-value-item" id="<%= it.id %>">
<% if(it.label) { %>
    <label class="label">
        <%= it.label %> / <span class="label__span">Text </span> <br>
    </label>
<% } %>
<% if(it.value) { %>
    <span class="span">
        <%= it.value %>
    </span>
<% } %>
</div>